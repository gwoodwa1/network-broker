version: "2"

run:
  timeout: 5m
  tests: true
  allow-parallel-runners: true

linters:
  default: none
  enable:
    - asasalint
    - asciicheck
    - bidichk
    - bodyclose
    - containedctx
    - contextcheck
    - copyloopvar
    - cyclop
    - decorder
    - depguard
    - dogsled
    - dupl
    - durationcheck
    - errcheck
    - errchkjson
    - errorlint
    - exhaustive
    - fatcontext
    - funlen
    - gocheckcompilerdirectives
    - gochecksumtype
    - gocognit
    - gocritic
    - gosec
    - govet
    - ineffassign
    - intrange
    - loggercheck
    - maintidx
    - makezero
    - mirror
    - misspell
    - nilerr
    - nilnesserr
    - noctx
    - nolintlint
    - prealloc
    - reassign
    - recvcheck
    - revive
    - rowserrcheck
    - sloglint
    - spancheck
    - sqlclosecheck
    - staticcheck
    - unconvert
    - unparam
    - unused
    - usestdlibvars
    - wastedassign
    - whitespace
  settings:
    cyclop:
      max-complexity: 20
      package-average: 12
    depguard:
      rules:
        secure_dependencies:
          list-mode: lax
          files:
            - $all
          deny:
            - pkg: crypto/des
              desc: DES and Triple DES are obsolete; use an authenticated modern cipher.
            - pkg: crypto/md5
              desc: MD5 is unsuitable for security-sensitive integrity checks.
            - pkg: crypto/rc4
              desc: RC4 is cryptographically broken and must not be used.
            - pkg: crypto/sha1
              desc: SHA-1 is unsuitable for security-sensitive integrity checks.
            - pkg: github.com/golang/protobuf
              desc: Use google.golang.org/protobuf instead of the deprecated module.
            - pkg: io/ioutil
              desc: Use the io and os packages instead of deprecated io/ioutil APIs.
            - pkg: math/rand
              desc: Use crypto/rand for security values or math/rand/v2 for simulations.
    errcheck:
      check-type-assertions: true
      check-blank: true
      exclude-functions:
        - fmt.Fprint
        - fmt.Fprintf
        - fmt.Fprintln
    exhaustive:
      default-signifies-exhaustive: true
    funlen:
      lines: 80
      statements: 50
    gocognit:
      min-complexity: 20
    gocritic:
      enable-all: true
      disabled-checks:
        - hugeParam
        - rangeValCopy
    govet:
      enable-all: true
      disable:
        - fieldalignment
        - shadow
    maintidx:
      under: 20
    nolintlint:
      allow-unused: false
      require-explanation: true
      require-specific: true
    revive:
      enable-all-rules: true
      rules:
        - name: add-constant
          disabled: true
        - name: argument-limit
          disabled: true
        - name: cognitive-complexity
          disabled: true
        - name: confusing-naming
          disabled: true
        - name: confusing-results
          disabled: true
        - name: cyclomatic
          disabled: true
        - name: exported
          disabled: true
        - name: function-length
          disabled: true
        - name: function-result-limit
          disabled: true
        - name: line-length-limit
          disabled: true
        - name: max-public-structs
          disabled: true
        - name: package-comments
          disabled: true
        - name: unnecessary-format
          disabled: true
        - name: unhandled-error
          disabled: true
        - name: use-errors-new
          disabled: true
        - name: use-slices-sort
          disabled: true

  exclusions:
    generated: strict
    rules:
      - path: _test\.go
        linters:
          - dupl
          - funlen
          - maintidx

formatters:
  enable:
    - gofumpt
  settings:
    gofumpt:
      module-path: network_broker
      extra-rules: true

issues:
  max-issues-per-linter: 0
  max-same-issues: 0

output:
  sort-order:
    - linter
    - file
